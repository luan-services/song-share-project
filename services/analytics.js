// função placeholder para iniciar cookies 'não-essenciais' para caso o usuário dê consentimento de cookies totais. (mesmo que esse projeto não use cookies, a função está preparada)

export const initAnalytics = () => {
	// codigo aqui
	return;
	console.log("Consentimento recebido, analytics ativado.");
};

export const logAnalyticsEvent = (eventName) => {
	// codigo aqui
	return;
	console.log(`Evento registrado: '${eventName}'`);
};